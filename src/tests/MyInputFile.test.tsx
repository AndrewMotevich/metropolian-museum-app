import { render } from '@testing-library/react';
import MyInputFile from '../components/MyForm/input-file/MyInputFile';
import { useRef } from 'react';

describe('Cards', () => {
  it('render Cards component', () => {});
});
